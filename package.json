{
  "name": "estore-pw",
  "version": "1.0.0",
  "description": "estore autotest",
  "main": "test.js",
  "scripts": {
    "test": "jest"
  },
  "author": "bigben",
  "license": "ISC",
  "jest": {
    "testMatch": [
      "**/tests/**/*.[jt]s?(x)",
      "**/?(*.)+(spec|test).[jt]s?(x)"
    ],
    "setupFilesAfterEnv": [
      "expect-playwright"
    ],
    "runner": "jest-serial-runner",
    "reporters": [
      "default",
      "jest-html-reporters"
    ]
  },
  "devDependencies": {
    "expect-playwright": "^0.3.3",
    "jest": "^26.6.3",
    "jest-html-reporters": "^2.1.0",
    "jest-playwright-preset": "^1.4.0",
    "jest-serial-runner": "^1.1.0",
    "playwright": "^1.10.0",
    "playwright-cli": "^0.170.0",
    "playwright-core": "^1.10.0"
  },
  "dependencies": {
    "playwright-webkit": "^1.10.0"
  }
}
